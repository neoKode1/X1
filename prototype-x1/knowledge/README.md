# ARIA Knowledge Base

Drop .txt, .md, .json, .csv, .py, or .log files here.
They will be chunked and ingested into ARIA's knowledge store.

Use: POST /knowledge/ingest to process all files in this folder.
Use: POST /knowledge/upload to upload a single file via API.
