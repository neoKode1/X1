# Robot Brain — Phase 2 Dependencies
# Install: pip install -r requirements.txt

# ── LLM Providers ──────────────────────────────────────────────────────────
# Install only the one(s) you need.
anthropic>=0.40.0        # Claude (default)
openai>=1.50.0           # GPT-4o / o1 (optional)

# ── Vision (Phase 2) ───────────────────────────────────────────────────────
# On Pi 5, prefer the system OpenCV:  sudo apt install python3-opencv
# The pip package below works everywhere including Mac/Linux dev machines.
opencv-python>=4.9.0
Pillow>=10.0.0

# ── Testing ────────────────────────────────────────────────────────────────
pytest>=8.0.0

# ── Raspberry Pi 5 GPIO (install on Pi only, not on dev machine) ───────────
# RPi.GPIO is NOT compatible with Pi 5. Use gpiozero + lgpio instead.
#
# On Pi 5:
#   sudo apt install python3-lgpio
#   pip install gpiozero lgpio
#
# Uncomment below to pin versions in requirements:
# gpiozero>=2.0
# lgpio>=0.2.2.0

